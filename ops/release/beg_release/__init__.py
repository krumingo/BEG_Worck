"""W0-09A release foundation: exact-version artifacts, release manifest, verification.

Standard library only, so the same code runs on the build PC and on the Synology host
inside a network-less ``python:3.11-slim`` container.
"""

TOOL_VERSION = "beg_release/1"
