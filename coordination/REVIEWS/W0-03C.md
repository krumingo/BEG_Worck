# Independent review — W0-03C / PR #20

Task-ID: W0-03C
PR: https://github.com/krumingo/BEG_Worck/pull/20
Head-SHA: be8cd207c94388e81b24173af6d690d69abcea00
Base-SHA: 2d03f8acb3ef1754fe66066064bc86bfe4872822
Verdict: CHANGES_REQUESTED

## Verified findings

1. `backend/app/master_data/index_bootstrap.py` lines 218–222 create a unique index before writing its claim to the run ledger. Simulated interruption after `create_index` but before `ledger.update_one` leaves the index present while the ledger remains `building` with `created=[]`. Existing rollback cannot identify that index from the ledger. This contradicts the stated crash-recovery guarantee.
2. Lines 237–252 ignore an error in the final ledger status update via `_ledger_set` and still return `STATUS_APPLIED`. A simulated final-write-only OSError leaves `status=building` with created index recorded, yet returns APPLIED.
3. The documented `sudo env POST_VERIFY_HOOK=...` command lacks a DSM Docker path; the first NAS attempt exited 2 before restore with `docker not found`. Correct the runbook/hook example without rerunning NAS.

## Evidence and limits

Review used the actual PR diff at exact head and inspected source lines. A Python async fake DB/ledger fault-injection harness run from a `git archive` of this SHA reproduced (1) and (2): after simulated KeyboardInterrupt, `index_exists=True, ledger_status=building, ledger_created=[]`; after simulated final OSError, `return_status=APPLIED, index_exists=True, ledger_status=building`. This is simulation, not a real-Mongo crash test. Existing 17/17 real-Mongo tests and NAS CLEAN are separate evidence; neither covers these fault windows. NAS CLEAN applies only to the 22 Sep archive with 9 legacy documents and no canonical md_* documents.

## Requested correction scope

Keep PR #20 Draft and the same Task-ID. Make the ledger recovery/rollback safe for an interruption window, fail closed on final ledger-write failure, add focused positive and forbidden fault-injection tests, and fix only the broken command example. Re-run relevant tests, show exact new SHA and HANDOFF. Do not touch NAS/Atlas/production, create real indexes there, merge, deploy, change FLOW business rules, or claim Implementation Gate PASS. Codex will review the new exact head independently.