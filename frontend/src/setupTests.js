// Loaded automatically by Create React App before every test file.
// jest-dom adds the DOM assertions the tests read best with:
// toBeInTheDocument, toBeDisabled, toHaveTextContent.
import "@testing-library/jest-dom";
